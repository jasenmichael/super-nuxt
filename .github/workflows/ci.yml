name: CI/CD

on:
  push:
    branches:
      - main
      # - master
  pull_request:
    branches:
      - main
      # - master

jobs:
  ci:
    runs-on: ${{ matrix.os }}

    strategy:
      matrix:
        os: [ubuntu-latest]
        node: [14]

    steps:
      - name: Checkout Prod 🛎
        uses: actions/checkout@master
        with:
          ref:

      - name: Checkout 🛎
        uses: actions/checkout@master

      - name: Setup node env 🏗
        uses: actions/setup-node@v2.1.2
        with:
          node-version: ${{ matrix.node }}

      - name: Cache node_modules 📦
        uses: actions/cache@v2
        with:
          path: ~/.npm
          key: ${{ runner.os }}-node-${{ hashFiles('**/package-lock.json') }}
          restore-keys: |
            ${{ runner.os }}-node-

      - name: Install dependencies 👨🏻‍💻
        run: npm ci

      - name: Run linter 👀
        run: npm run lint

      - name: Update files
        # run: npm run generate
        # git remote set-url origin https://x-access-token:${{ secrets.GITHUB_TOKEN }}@github.com/${{ github.repository }}
        run: |
          git config --local user.email "$(git log --format='%ae' HEAD^!)"
          git config --local user.name "$(git log --format='%an' HEAD^!)"
          git checkout prod -- static/img/ content/navigation.json content/tumblr.json 2>/dev/null
          sed -i 's/static\/img\/\*//g' .gitignore
          sed -i 's/content\/navigation.json//g' .gitignore
          sed -i 's/content\/tumblr.json//g' .gitignore
          npm run prebuild

      # in .gitignore replace static/img/* with ''
      - name: Commit files
        # if: steps.push-action.outputs.changes_detected == 'true'
        run: |
          git add -A 2>/dev/null
          git commit -m "updated files" -a 2>/dev/null

      - name: Push changes
        uses: ad-m/github-push-action@master
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          branch: prod
          # force: true

      # Deploy to local repo

      # - uses: stefanzweifel/git-auto-commit-action@v4
      #   with:
      #     commit_message: 'update files'
      #     # push_options: '--force'
      #     branch: main

      # - name: Deploy
      #   uses: mikeal/publish-to-github-action@master
      #   env:
      #     GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      #     BRANCH_NAME: 'prod'
